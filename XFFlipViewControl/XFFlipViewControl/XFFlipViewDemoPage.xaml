<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XFFlipViewControl.XFFlipViewDemoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xfFlipViewControl="clr-namespace:XFFlipViewControl;assembly=XFFlipViewControl">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness">
            <OnPlatform.iOS>
                0, 20, 0, 0
            </OnPlatform.iOS>
        </OnPlatform>
    </ContentPage.Padding>

    <Grid>

        <Label
            FontSize="20"
            HorizontalTextAlignment="Center"
            Text="Animated Flip View with pure Xamarin Forms!" />

        <xfFlipViewControl:XFFlipView x:Name="XFFlipViewControl1" Grid.Row="1">

            <xfFlipViewControl:XFFlipView.FrontView>
                <Frame
                    Margin="10"
                    Padding="0"
                    BackgroundColor="#0080ff"
                    CornerRadius="10"
                    HasShadow="True"
                    OutlineColor="Silver">
                    <Grid RowSpacing="5" VerticalOptions="CenterAndExpand">
                        <Label
                            Grid.Row="0"
                            FontSize="Large"
                            HorizontalTextAlignment="Center"
                            Text="This is Front View"
                            TextColor="White"
                            VerticalTextAlignment="Center" />

                        <Image
                            Grid.Row="1"
                            Source="{xfFlipViewControl:ImageResource XFFlipViewControl.Resources.monkey1.png}"
                            VerticalOptions="CenterAndExpand" />

                        <Label
                            x:Name="frontViewTimeLabel"
                            Grid.Row="2"
                            Margin="20,0,20,0"
                            FontAttributes="Bold"
                            FontSize="Medium"
                            HorizontalTextAlignment="Center"
                            TextColor="White" />

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="150" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                    </Grid>
                </Frame>
            </xfFlipViewControl:XFFlipView.FrontView>

            <xfFlipViewControl:XFFlipView.BackView>
                <Frame
                    Margin="10"
                    Padding="0"
                    BackgroundColor="#ff0080"
                    CornerRadius="10"
                    HasShadow="True"
                    OutlineColor="Silver">
                    <Grid RowSpacing="5" VerticalOptions="CenterAndExpand">
                        <Label
                            Grid.Row="0"
                            FontSize="Large"
                            HorizontalTextAlignment="Center"
                            Text="This is Back View"
                            TextColor="White"
                            VerticalTextAlignment="Center" />

                        <Image
                            Grid.Row="1"
                            Source="{xfFlipViewControl:ImageResource XFFlipViewControl.Resources.monkey2.png}"
                            VerticalOptions="CenterAndExpand" />

                        <Label
                            x:Name="backViewTimeLabel"
                            Grid.Row="2"
                            Margin="20,0,20,0"
                            FontAttributes="Bold"
                            FontSize="Medium"
                            HorizontalTextAlignment="Center"
                            TextColor="White" />

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="150" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                    </Grid>
                </Frame>
            </xfFlipViewControl:XFFlipView.BackView>

        </xfFlipViewControl:XFFlipView>

        <Button
            x:Name="moreButton"
            Grid.Row="2"
            Clicked="moreButton_OnClicked"
            Text="more of this" />

        <Button
            x:Name="flipItButton"
            Grid.Row="3"
            Clicked="flipItButton_OnClicked"
            Text="Flip it!" />

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
    </Grid>

</ContentPage>